import React from 'react';

const contextName = React.createContext();      // creation way of context
// export const parentContextName = React.createContext();      // creation way of context

export default contextName;
